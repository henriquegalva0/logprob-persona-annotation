# logprob-persona-annotation
Do a language model's internal token-level probabilities carry a reliable, non-redundant signal of persona drift under conversational stress, one that holds up across different stress mechanisms, generalizes to unseen models and persona sources, and remains consistent across different generator models and LLM judges?
